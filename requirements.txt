streamlit
PyYAML
xrpl-py
pandas
pandas-ta  # optional — Alpha TA uses pure-pandas fallback if unavailable
pytest

# For the real-time WS + pure A-S HUD (experimental/ws_feed/real_time_as_hud.py)
# and live testing of the committed path. Install with: pip install -r requirements.txt
fastapi
uvicorn[standard]
requests  # used for real Grok/xAI calls from the HUD Intelligence tab and model discovery
webauthn  # optional HUD passkey login (HTTPS); password auth works without it
