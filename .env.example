# Copy to .env in repo root (gitignored). Fill once — tester, HUD, and calibration pick it up.
XLG_GROK_KEY=xai-your-key-here
XLG_GROK_MODEL=grok-3
XLG_INTEL_AI_PROVIDER=grok
# Public HUD login (VPS with hud_bind_host: 0.0.0.0)
XLG_HUD_USERNAME=operator
XLG_HUD_PASSWORD=change-me-strong-password
